# Use docker-based build environment (instead of openvz)
sudo: false

language: java

# Cache eclipse
cache:
  directories:
    - '$HOME/eclipse-relang'

before_install: './scripts/download_eclipse.sh'
install: ant make-stable-p2
script: true